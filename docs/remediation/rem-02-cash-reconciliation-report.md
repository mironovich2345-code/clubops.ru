# REM-02 — Cash Reconciliation Report (dev)

Generated by `reconcile:cash-contours` + `preflight:cash-cutover` (both READ-ONLY). Machine data:
`docs/audits/data/cash-contours-reconciliation.json`, `docs/audits/data/cash-cutover-preflight.json`.

## Dev result
- Reconciliation: 8 clubs, 1 entity with a resolvable balance, **0 diverged** (dev cash tables are near-empty;
  the demo ООО has no legacy wallet → n/a).
- Preflight: 10 checks, **0 offending rows** (no duplicate active snapshots, no cross-tenant, no ООО cash
  expense, no post-cutover legacy write, no orphan LE).

## Interpretation
Dev proves the tooling runs read-only and the resolver/guard behave; it proves **nothing** about production
data. Before any production cutover, run both tools on a **production read replica** and record the real
canonical-vs-legacy differences per club/entity. The canonical (formula) number is the official balance;
legacy differences are historical and remediated only via the safe reconciliation classes (accountant-
confirmed correction records), never a destructive edit.

## Real integration proof
`test:rem-02-integration` 13/13 executes the actual resolver on a disposable DB: cancelled/corrected/backdated/
future snapshot handling, asOf history, cutover guard, legacy-independence, tenant isolation.
