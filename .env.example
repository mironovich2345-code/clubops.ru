DATABASE_URL="file:./dev.db"
SESSION_SECRET="change-me-in-production"

# AI document recognition (optional).
# Leave AI_PROVIDER empty (or unset) to use the safe mock fallback.
# Set AI_PROVIDER="openai" and provide OPENAI_API_KEY to enable real recognition.
AI_PROVIDER=""
OPENAI_API_KEY=""
# Optional model override (default: gpt-4o-mini)
# OPENAI_MODEL="gpt-4o-mini"
